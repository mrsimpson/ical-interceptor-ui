name: CI

on: 
  push:
    branches:
      - '!master'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: |
        npm i
    - run: |
        npm run test
    - run: |
        npm run build